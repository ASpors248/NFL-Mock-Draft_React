import React from 'react';
import { useSelector, useDispatch } from 'react-redux';

export function PickPanel(teams, prospects) {

    return (
        <div className="pick-panel">
            <p>picks go here.</p>
        </div>
    );
}